name: Publish-Artifacts

on:
  workflow_run:
    workflows: [Push-Actions-Test]
    types:
      - completed

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Hello
        run: echo "Hello, world!"
